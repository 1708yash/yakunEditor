import React from "react";

const ExtensionsComponent = React.memo(() => {
    return (
        <div className="route">
            Extensions route
        </div>
    )
})

export default ExtensionsComponent